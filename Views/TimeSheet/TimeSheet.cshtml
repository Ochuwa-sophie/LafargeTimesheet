@{
    ViewData["Title"] = "Timesheet";
}
<div>
    <div class="text-left">
            <input type="button"
         value="Check In"
         onclick=location.href= <%: Url.Action("Action", "Controller") %>
          
    </div>

    <div class="text-center">
            <input type="button"
         value="Check Out"
         onclick=location.href=<%: Url.Action("Action", "Controller") %>
    </div>
</div>
<div>
    
</div>